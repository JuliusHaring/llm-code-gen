name: Test Action

on:
  workflow_call:
    secrets:
      OPENAI_API_TOKEN:
        required: true
  workflow_dispatch:

jobs:
  test:
    name: Test Action
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Run Action Test
      uses: ./
      with:
        pat: ${{ secrets.PAT }}
        branch_name: 'test-ci-setup'
        descriptions: 'linting and formatting for python with flake8 and black, using github actions'
        openai_token: ${{ secrets.OPENAI_API_TOKEN }}
        python_log_level: 'DEBUG'
